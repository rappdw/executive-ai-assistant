LANGSMITH_API_KEY=...
OPENAI_API_KEY=...
ANTHROPIC_API_KEY=...

# Email Provider Configuration
# Set EMAIL_PROVIDER to either "gmail" or "exchange"
EMAIL_PROVIDER=gmail

# Gmail Configuration (required when EMAIL_PROVIDER=gmail)
GMAIL_SECRET=...
GMAIL_TOKEN=...

# Exchange/Microsoft 365 Configuration (required when EMAIL_PROVIDER=exchange)
# These values are obtained from your Azure AD app registration
EXCHANGE_TENANT_ID=your-azure-tenant-id-here
EXCHANGE_CLIENT_ID=your-azure-app-client-id-here  
EXCHANGE_CLIENT_SECRET=your-azure-app-client-secret-here

# Migration Guide:
# For existing Gmail installations: No changes needed, system defaults to Gmail
# For new Exchange installations: 
#   1. Set EMAIL_PROVIDER=exchange
#   2. Register an Azure AD application with Mail.Read, Mail.Send, Calendars.ReadWrite permissions
#   3. Set the three EXCHANGE_* variables above with your Azure AD app details